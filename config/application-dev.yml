application:

server:
  port: ${PORT:8086}

  otp:
    length: ${OTP_LENGTH:6}
mailing:
      frontend:
        activation-url: ${ACTIVATION_URL:http://localhost:8086/auth/activate-account}
#        reset-url: ${RESET_URL:http://95.111.225.198:8283/sendNewPassword-password?token=} // version deploye
  #      reset-url: ${RESET_URL:http://localhost:3000/sendNewPassword-password?token=} // en locale



spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:Ecommerce}
    username: ${DB_USERNAME:test}
    password: ${DB_PASSWORD:test@2023}

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mvc:
    hiddenmethod:
      filter:
        enabled: true
    pathmatch:
      matching-strategy: ant_path_matcher  # Autoriser des formulaires à déclencher une requête DELETE, PUT, etc., même si ce n’est pas prévu nativement par HTML.

  main:
   allow-bean-definition-overriding: true  # Quand tu as plusieurs beans avec le même nom dans ton application, Spring lève une erreur par défaut pour éviter toute ambiguïté.
  #Mais si tu ajoutes cette propriété à true, alors le dernier bean déclaré écrasera les autres sans erreur.
  mail:
    host: smtp.gmail.com
    port: ${MAIL_PORT:587}
    username: ${EMAIL_USERNAME:steffjefferson404@gmail.com}
    password: ${PASSWORD:iuce ukzn wirx ccsj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          debug: true

